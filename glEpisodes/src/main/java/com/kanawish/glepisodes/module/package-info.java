/**
 * ScopeBuilder is responsible for building DI Scopes
 *
 * Modules are installed in a Scope, and they
 * define the Bindings of Component class-to-instances.
 *
 * Components reside in a scope package (e.g. `module.activity` or `module.app`)
 *
 * If named dependencies are needed, those would reside under `module.names`
 */
package com.kanawish.glepisodes.module;
